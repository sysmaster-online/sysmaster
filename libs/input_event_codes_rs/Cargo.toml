[package]
name = "input_event_codes_rs"
version = "0.2.4"
edition = "2021"

[build-dependencies.bindgen]
default_features = false
features = ["runtime"]
version = "0.63.0"

[build-dependencies]
basic = { path = "../basic" }

[dependencies]
libc = "0.2.*"
